import { createTypes } from '../actionTypes';

export const GET_USER_PROFILE = createTypes('PROFILE/GET_USER_PROFILE');

export const UPDATE_USER_PROFILE = createTypes('PROFILE/UPDATE_USER_PROFILE');
