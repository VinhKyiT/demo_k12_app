# Fastlane Android Instruction

### Git
git tag <tag name>
git push

### Fastlane
cd android

bundle update
bundle update fastlane
bundle exec fastlane update_plugins

bundle exec fastlane buildRelease
